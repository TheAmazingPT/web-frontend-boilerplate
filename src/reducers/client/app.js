/**
 * Initial state
 * @type {Array}
 */
const initialState = {};

/**
 * Handle the FAST log changes
 * @param  {Array} state The old state
 * @param  {Object} action The action to apply
 * @return {Array} The new state
 */
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
